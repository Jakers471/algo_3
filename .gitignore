# Virtual environment
venv/
.venv/
env/

# Python cache
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Environment / secrets (API keys, etc.)
.env
*.key
secrets.json

# Data & logs (root-anchored so src/data/ code is NOT ignored)
*.log
/data/
/logs/

# Backtest run outputs (labeled runs: trades, summary, equity.png, manifest)
/runs/

# Cached derived series + the packed chart bar cache
/cache/

# Recorded live market sessions (raw JSONL from the SignalR market hub)
/capture/

# Scratch — disposable spikes & experiments (README is force-added)

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
