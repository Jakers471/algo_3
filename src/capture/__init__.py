"""Record and inspect the live market feed.

``recorder`` writes raw hub events verbatim; ``inspect`` reads a recording back
and reports what the feed actually contains. The captures themselves live in
the top-level ``capture/`` folder (git-ignored) - data, not code.
"""
