"""Logging area: the dial values (settings) and the setup that applies them.

Import the piece you need, e.g. ``from src.logging import setup`` to call
``setup_logging()`` once at startup, or ``from src.logging import settings``
for the level/destination dials.
"""
