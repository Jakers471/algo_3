"""CLI area: thin doors that parse input, call a real engine, format output.

Doors hold no trading logic - each just wires a command to the module that
does the work. Run one with ``python -m src.cli.<name>``.
"""
