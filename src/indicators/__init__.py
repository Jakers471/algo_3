"""Indicators: state machines over the event stream.

base defines what an indicator is; registry orders and runs them.
Each indicator is one file, named for its id, with its dials in
src/config/indicators/<id>.py.
"""
