"""Reporting area: turn a backtest's Trades into saved, shareable artifacts.

One module per output: stats.py (All/Long/Short metrics), trades.py (the trade
list), equity.py (the equity/drawdown image), console.py (the terminal
summary), and run.py (bundle everything into a labeled, replayable run folder).
"""
