# Core dependencies for algo_3
# Pin versions with == once you settle on them (pip freeze > requirements.txt)

# Data & numerical
pandas
numpy
pyarrow          # read/write Parquet (the NT8 historical data)

# Backtesting / research
vectorbt

# Plotting (equity/drawdown images for backtest runs)
matplotlib

# HTTP / API access
requests

# Config / secrets
python-dotenv

# Testing
pytest

# --- add as you build ---
# ccxt            # crypto exchange APIs
# ta-lib          # technical indicators
